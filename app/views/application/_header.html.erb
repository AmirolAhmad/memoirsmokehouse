<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Navbar</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto mb-2 mb-md-0">
        <li class="nav-item">
          <%= link_to "Home", root_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Menu</a>
        </li>
        <li class="nav-item">
          <%= link_to "New Order", new_order_path, class: "nav-link" %>
        </li>
        <% if !user_signed_in? %>
          <%= link_to login_path, data: { turbolinks: false }, class: "nav-link" do %>
            <i class="icon-user-male"></i> Sign in
          <% end %>
        <% else %>
          <%= link_to admin_master_path, data: { turbolinks: false }, class: "nav-link" do %>
            <i class="icon-home"></i> My Dashboard
          <% end %>
          <%= link_to destroy_user_session_path, method: :delete, class: "nav-link", data: { turbolinks: false } do %>
            <i class="sign-out"></i> Sign out
          <% end %>
        <% end %>
      </ul>
      <%= form_tag(search_path, method: :get, class: "d-flex") do %>
        <%= text_field_tag :search, params[:search], class: "form-control me-2" %>
        <%= submit_tag ("search"), class: "btn btn-outline-success" %>
      <% end %>
    </div>
  </div>
</nav>